{
  "name": "section-3",
  "version": "1.0.0",
  "description": "1. go to file 2. at terminal prompt, \"npm init -y\"  3. at terminal prompt, \"npm install react react-dom next\" 4. add scripts to package.json under main     \"scripts\": {     \"test\": \"echo \\\"Error: no test specified\\\" && exit 1\",     \"dev\": \"next\",     \"build\": \"next build\",     \"start\": \"next start\"   } 5. at terminal prompt, \"mkdir pages\" 6. create \"index.js\" file within \"pages\" directory. base content as follows:     import React from 'react'     const InputElement = () => {",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "next",
    "build": "next build",
    "start": "next start"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "next": "^10.0.6",
    "react": "^17.0.1",
    "react-dom": "^17.0.1"
  }
}
